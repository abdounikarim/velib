/**
 * @typedef {Object} UIAdapter
 * @property {() => void}            initSidenav  - Initialize the mobile side navigation
 * @property {() => void}            initCarousel - Initialize the image carousel
 * @property {(msg: string) => void} showAlert    - Display an alert message
 */
